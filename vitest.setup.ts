// Shared vitest setup. Component tests opt into the DOM with a `// @vitest-environment jsdom`
// docblock at the top of the file; this registers the jest-dom matchers for them.
import "@testing-library/jest-dom/vitest";
